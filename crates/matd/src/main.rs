//! `matd` — 常駐レイヤ（Phase 4, 未実装スタブ）。
//!
//! 温かい CASE セッションを保持し、`mat-core` 経由で chip-tool を駆動する常駐
//! デーモン。クロスプロトコルの `casad` とは別物。設計は ARCHITECTURE.md を参照。
//! 現状はワークスペースの枠だけ確保したプレースホルダ。

fn main() {
    eprintln!("matd is not implemented yet (Phase 4). See ARCHITECTURE.md.");
    std::process::exit(1);
}
